import "../style.css";
import { useEffect } from "react";
import { useNavigate } from "react-router-dom";
import Header from "../components/Header";

const Home = () => {
  return (
    <>
      <Header user_name={null} image_url={null} profile_id={null} />
    </>
  );
};

export default Home;
